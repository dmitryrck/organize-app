<tr id="<%= dom_id purchase %>">
  <td>
    <% if purchase.tag %>
      <span class="label label-info"><%= purchase.tag %></span>
    <% end %>
    <%= link_to purchase, purchase %>
  </td>
  <td><%= currency purchase.value %></td>
  <td><%= purchase.purchased_at %></td>
</tr>
